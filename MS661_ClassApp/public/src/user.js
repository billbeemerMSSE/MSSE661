const doUpdateUsername = async (e) => {
  e.preventDefault();
  user._updateUserNameEventHandler();
};

const doUpdatePassword = async (e) => {
  e.preventDefault();
  user._updatePasswordEventHandler();
};